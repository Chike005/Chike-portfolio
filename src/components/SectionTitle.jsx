function SectionTitle({ title }) {
  return (
    <h2 className="text-4xl font-bold text-dark mb-8 border-b-2 border-primary inline-block">
      {title}
    </h2>
  );
}

export default SectionTitle;
